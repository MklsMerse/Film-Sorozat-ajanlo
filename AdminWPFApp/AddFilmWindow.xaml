<Window x:Class="AdminWPFApp.AddFilmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdminWPFApp"
        mc:Ignorable="d"
        Title="Film hozzáadása" Height="450" Width="400" Background="#F4F4F4">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#D3D3D3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#A8D08D" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Margin" Value="0,10" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Cím:"/>
            <TextBox x:Name="CimTextBox" Margin="0,5"/>

            <TextBlock Text="Leírás:"/>
            <TextBox x:Name="LeirasTextBox" Margin="0,5" AcceptsReturn="True" Height="80"/>

            <TextBlock Text="Megjelenési dátum (YYYY-MM-DD):"/>
            <TextBox x:Name="DatumTextBox" Margin="0,5"/>

            <TextBlock Text="Műfaj:"/>
            <TextBox x:Name="MufajTextBox" Margin="0,5"/>

            <TextBlock Text="Rendező:"/>
            <TextBox x:Name="RendezoTextBox" Margin="0,5"/>

            <TextBlock Text="Szereplők:"/>
            <TextBox x:Name="SzereplokTextBox" Margin="0,5"/>

            <TextBlock Text="Értékelés:"/>
            <TextBox x:Name="ErtekelesTextBox" Margin="0,5"/>

            <TextBlock Text="Film URL:"/>
            <TextBox x:Name="FilmUrlTextBox" Margin="0,5"/>

            <Button Content="Film hozzáadása" Margin="0,10" Click="AddFilmButton_Click"/>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
